interface IApplicationWrapperProps {
  children: JSX.Element;
  hasPadding: boolean;
}

interface IStyledApplicationWrapperProps {
  hasPadding: boolean;
}

export type { IApplicationWrapperProps, IStyledApplicationWrapperProps };
